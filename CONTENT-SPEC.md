# CONTENT-SPEC — 内容规范 v2.0

> 版本：v2.0 ｜ 日期：2026-08-14 ｜ 依据：DESIGN-SPEC.md（唯一事实源，冲突时以它为准）
> v2.0 变更：词条升级为「知识树节点」——新增 dimension/granularity/relations 字段；
> 正文新增 原理/由来与历史/关系图谱 区块；确立目录层级 = 知识树的物理实现。

本文档定义 AIGC 术语知识库的内容标准：词条长什么样、怎么写、怎么分级、怎么连线、怎么校验。所有进入 `docs/词条/` 的内容都必须符合本规范。

---

## 〇、核心设计：词条是知识树的节点

**词条不是孤立的字典条目，而是知识树上的节点。**

```
树 = 节点（词条）+ 边（关系）
垂直：粒度链（dimension 相同，granularity 递进）—— 厂商 → 模型家族 → 变体
水平：平行组（dimension + granularity 都相同）—— 同一层的兄弟词条，可对比可并列
```

- **同一 dimension（场景/比较轴）+ 同一 granularity（粒度）= 同层兄弟**，站点自动归组展示
- 对比关系只在同层有意义：`bf16` 对比 `fp32` ✅；`bf16` 对比 `MiniMax` ❌
- 目录结构（文件夹层级）= 知识树的物理实现，见第八节

---

## 一、词条 Schema（每条强制字段）

每个词条是一个独立的 `.md` 文件，文件名 = 术语名（如 `LoRA.md`），开头是 YAML frontmatter，后面是正文区。

```yaml
---
term: LoRA                          # 术语名（英文原名）
full_name: Low-Rank Adaptation      # 全称
aliases: [低秩适配]                 # 别名
category: 模型微调                  # 分类（第五节，11 类之一）
dimension: 微调技术                  # 场景/比较轴：和谁并列对比
granularity: 具体技术                # 粒度层级：厂商/模型家族/变体/技术/格式...
maturity: stable                    # 成熟度: stable(🟢) / hot(🟡)
status: published                   # 状态: draft / review / published
contributors: []                    # 贡献者（GitHub 用户名）
sources:                            # 来源链接（强制，无源不收录）
  - https://arxiv.org/abs/2106.09685
relations:                          # 关系（核心！语义接近即链接）
  - target: 微调
    note: LoRA 是微调的一种实现，语义紧邻
  - target: 全量微调
    note: 常拿来对比的另一个微调方案
  - target: PEFT
    note: 同属参数高效微调家族
  # type 可选：想标注关系类型（prerequisite/contrast/peer/...）就写，不强制
  # - type: contrast
  #   target: xxx
  #   note: xxx
created: 2026-08-14
updated: 2026-08-14
revisions: 1                        # 修订次数
---

## 定义
（一段话，人话优先，先大白话后严谨）

## 原理
（为什么这样工作：底层机制，2-4 段。若属于表层概念，写 2-3 句 + 链接到底层词条，不重复展开）

## 由来与历史
（谁发明的、为什么出现、怎么演进的，2-4 段）

## 应用
（在 ComfyUI / 实际工作流里怎么用，含参数/后缀/注意事项）

## 争议 / 讨论（如有）
（社区争议点，正反观点 + 各自来源）

## 参见
（文末自然链接列表，像 Wikipedia 底部那样。把 relations 翻译成自然的"相关词条"，按重要性排序，直接 [词条名](词条.md) 形式，不加标签不加表格）

---

## 一·五、渲染原则：结构在代码层，文字给读者（🔴 核心）

**frontmatter（dimension/granularity/relations）是代码层的坐标与路由，给机器和 Agent 用；渲染出来的页面必须是干净的人话文章。**

| 信息 | 代码层（frontmatter/目录） | 渲染层（读者看到的） |
|------|--------------------------|---------------------|
| 层级坐标 | `dimension` + `granularity` 字段 | ❌ 不显示标签，读者无需知道 |
| 关系 | `relations` 数组（类型+方向） | 只渲染成文末「参见」的自然链接 |
| 成熟度 | `maturity: stable/hot` | 🟡 热词警示条（必须）；🟢 不显示徽章 |
| 目录层级 | 文件夹结构（01-底层原理/…） | 正常导航（面包屑可选） |

**规则**：
- 读者打开词条页，看到的是：标题 → 正文（定义/原理/历史/应用）→ 文末相关链接。没有关系表格、没有类型标签、没有坐标面板
- 层级、关系、分类全部藏在代码里，渲染时隐去
- 目录/面包屑可以显示，因为那是正常网站导航（就像 Wikipedia 有分类页），但不用每个词条挂满元数据
- 机器可读性靠 frontmatter + CI 校验保证，不靠渲染暴露
```

---

## 二、关系（v1：语义接近即链接）

关系是词条的灵魂——**写词条必须同时声明关系，写一个连一个**。但不需要精确定义关系类型：

- **必填**：`target`（指向哪个词条）+ `note`（一句话说明为什么接近——这是给读者看的，也是将来机器学习路径的原料）
- **可选**：`type` 想标注就标注（见下方类型参考），不标不校验。关系模糊是常态，LoRA 和微调既是前置又是包含，硬分类反而失真
- **本质**：`relations` 就是"语义接近度"的显式声明——两个概念在向量空间里靠得近，就在代码里连一条边。机器将来可按被引用密度 / note 语义自动补链

**类型参考（可选标注，不强制）**：

| 类型 | 含义 | 例子 |
|------|------|------|
| `prerequisite` | 前置依赖 | 扩散模型 → 去噪 |
| `part_of` | 属于某个家族 | UNet ∈ 扩散模型 |
| `peer` | 同层平行 | MiniMax ↔ 阿里 |
| `contrast` | 对比 | bf16 vs fp32 |
| `evolved_from` | 演进 | int8 ← 蒸馏量化 |
| `applies_to` | 应用于 | LoRA → 风格模型 |
| `similar_to` | 相似但不同 | GGUF vs safetensors |

**规则**：
- `peer` 若标注，两端应 `dimension` + `granularity` 相同
- 一个词条没有 `relations` = 孤岛，不合格（要么没写全，要么概念过气该删）

---

## 三、写作风格规范

一句话原则：**读者是来学东西的，不是来考英语和术语猜谜的。**

- **人话优先**：定义区先用一句大白话讲清楚「这是什么」，再补充严谨说法。宁可啰嗦一点，不要炫技。
- **术语写法**：中文为主，术语保留英文原文 + 全称。首次出现写「LoRA（Low-Rank Adaptation，低秩适配）」，后文直接用英文简称即可。
- **避免堆砌**：一段里出现超过 3 个陌生术语，就要停下来解释，或加参见链接。术语出现而不解释，等于劝退读者。
- **面向大众**：假设读者懂一点 AI 概念、但不是专家。能举例就举例，能打比方就打比方。
- **来源留痕**：关键论断（尤其是争议观点）必须能对应到 `sources` 里的链接。这不是学术论文，但不能凭空说话。
- **不写营销话**：不说「革命性」「吊打」「碾压」，只说它解决了什么问题、代价是什么。
- **渐进披露**：表层词条不重复底层原理——原理写 2-3 句 + 链到底层词条，想深挖自己爬树。

---

## 四、成熟度分级

| 级别 | 状态 | 允许 | 必须标注 |
|------|------|------|---------|
| 🟢 已沉淀 | `stable` | 完整定义 + 多源交叉验证 | 无 |
| 🟡 社区热词 | `hot` | 一句话定义 + 单一来源 | 「🟡 社区热词，未经沉淀，信息可能快速过时」 |

- 新概念默认从 `hot` 进，先占坑、讲清楚是什么，不追求完备。
- **升级规则**：`hot → stable` 需要 ≥2 个独立来源 + 维护者审校通过（详见 MAINTENANCE.md）。
- `hot` 词条的定义区顶部必须放热词警示条，一个字都不能少。

---

## 五、校验检查清单（提交前逐条自查）

Schema 校验由 CI 自动执行，缺失即打回。提交词条前请对照以下清单：

**必填字段**
- [ ] `term` 已填，且与文件名一致（如 `LoRA.md` 对应 `term: LoRA`）
- [ ] `full_name` 已填（术语的英文全称）
- [ ] `category` 已填，且属于本规范第六节分类体系之一
- [ ] `dimension` 已填（场景/比较轴）
- [ ] `granularity` 已填（粒度层级）
- [ ] `maturity` 已填（`stable` 或 `hot`）
- [ ] 「定义」区非空（一段话）
- [ ] 「原理」区非空（表层词条至少 2-3 句 + 底层链接）
- [ ] 「由来与历史」区非空（至少 1 段）
- [ ] `sources` 至少 1 条有效 URL（可点击、可验证，死链打回）
- [ ] `relations` 至少 1 条（无关系 = 孤岛 = 打回）
- [ ] `relations[].target` 已填（指向存在的词条或待创建清单）
- [ ] `relations[].note` 已填（一句话说明为什么接近）

**格式与一致性**
- [ ] `maturity: hot` 的词条，定义区顶部已标注「🟡 社区热词，未经沉淀，信息可能快速过时」
- [ ] 若标注了 `relations[].type`，属于参考类型之一（不标不校验）
- [ ] 若标注了 `peer` 类型，两端 `dimension` + `granularity` 相同
- [ ] 文件名无特殊字符（空格、`/`、`:`、`?` 等不允许）
- [ ] `created` / `updated` 为 `YYYY-MM-DD` 格式

**写作质量（人工审校时看）**
- [ ] 定义区人话优先，一段话能看懂
- [ ] 术语首次出现带英文全称
- [ ] 无堆砌术语、无营销话术

---

## 六、分类体系（v1，11 类）

`category` 必须从下表选择。

| 分类 | 收录什么 | 示例 |
|------|---------|------|
| 模型 | 具体模型与模型家族 | GPT、Qwen、Stable Diffusion |
| 架构 | 网络结构与模块设计 | Transformer、MoE、注意力机制 |
| 训练 | 训练方法与范式 | 预训练、RLHF、蒸馏 |
| 推理 | 推理运行与优化 | KV Cache、量化、推理引擎 |
| 模型微调 | 参数高效的微调技术 | LoRA、PEFT、全量微调 |
| 工具 | 开发与使用工具 | ComfyUI、Ollama、llama.cpp |
| 平台 | 模型托管与服务平台 | Hugging Face、ModelScope、vLLM 云端 |
| 对齐 | 价值观与安全对齐 | 对齐、Constitutional AI、越狱 |
| 上下文工程 | 提示词与上下文管理 | Prompt、RAG、上下文窗口 |
| 多模态 | 文/图/音/视频生成与理解 | VAE、Diffusion、CLIP |
| Agent | 智能体与工具调用 | Function Calling、MCP、ReAct |

新增分类需修改本文件并在 PR 中说明理由；词条没有合适分类时，优先归到最接近的，不要随手开新类。

---

## 七、dimension / granularity 速查（AIGC 域）

`dimension`（场景轴）+ `granularity`（粒度）共同决定词条在知识树里的坐标。参考值：

| dimension（场景） | granularity 层级（细→粗） | 例子 |
|-------------------|--------------------------|------|
| 模型厂商 | 厂商 | MiniMax、阿里、BFL、Stability AI |
| 模型家族 | 家族 → 具体模型 → 变体 | Wan → Wan2.1 → 1.3B / 14B |
| 量化格式 | 格式 | bf16、fp16、fp8、int8、GGUF、safetensors |
| 加速技术 | 技术 | 蒸馏、Turbo、Lightning、SageAttention |
| 微调技术 | 技术 | LoRA、全量微调、PEFT |
| 组件 | 组件 | UNet、VAE、CLIP、文本编码器 |
| 采样参数 | 参数 | steps、CFG、scheduler、seed、denoise |
| 生成任务 | 任务 | 文生图、图生图、文生视频、图生视频 |

同 dimension + 同 granularity = 兄弟节点；dimension 相同 granularity 递进 = 父子链。

---

## 八、目录层级 = 知识树的物理实现

`docs/词条/` 按粒度分层建子目录，**文件夹层级就是树**：

```
docs/词条/
├── 01-底层原理/          ← 根：原理层（写全：历史+原理+应用）
│   ├── 神经网络.md
│   ├── 浮点数与精度.md
│   └── 扩散模型.md
├── 02-组件/              ← 枝：模型组件
│   ├── UNet.md
│   ├── VAE.md
│   └── CLIP.md
└── 03-表层概念/          ← 叶：具体技术/格式/参数（原理简述+挂链）
    ├── 量化格式/
    │   ├── bf16.md
    │   └── int8.md
    └── 微调/
        └── LoRA.md
```

**规则**：
- 新词条按 `granularity` 放入对应层级目录
- 目录名带序号（01-、02-…）保证展示顺序 = 学习顺序
- `docs/地图.md` 用 Mermaid mindmap 渲染整棵树（从 frontmatter 自动生成，树永远和词条同步）

---

## 九、地图页规范（docs/地图.md）

- 顶部：Mermaid mindmap 整树图（给人看全貌）
- 中部：按 dimension 分组列出所有词条 + 成熟度标记
- 底部：孤岛词条清单（无 relations 的，督促补链或删除）
- 地图页由 CI 自动生成/校验，不手写（见 MAINTENANCE.md）
