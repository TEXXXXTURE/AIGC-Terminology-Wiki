你是前端/UI 设计师。项目在 D:/AIGC术语库/，是一个 MkDocs Material 的术语知识库站点。

第一步：完整读取以下文件了解现状：
1. D:/AIGC术语库/mkdocs.yml（主题配置）
2. D:/AIGC术语库/docs/stylesheets/extra.css（现有自定义样式）

背景：站点当前是"深色星空 + HF 橙粉"的过渡状态。用户要求整体视觉向 HuggingFace（huggingface.co）靠拢：
- HF 品牌色：#FF9D0B（主橙）、#FFD21E（亮黄）、#FF0789（粉）、#21DE75（绿）、#F94040（红）、#097EFF（蓝）、#861FFF（紫）
- HF 风格：多彩、活泼、圆角卡片、emoji 点缀（🤗🧨🎬）、信息层次清晰
- 用户可以访问 https://huggingface.co 和 https://huggingface.co/learn 看参考

任务：系统性优化站点视觉，输出修改后的 extra.css（和必要时的 mkdocs.yml 调整）：

1. **整体色调向 HF 靠拢**：保留深色星空底（用户喜欢），但所有强调色、链接、导航、表格、代码块、滚动条、按钮统一用 HF 色系；primary 已设 orange，检查 accent 是否需要调
2. **emoji 风格**：HF 用 emoji 做视觉点缀（不是图标字体）。检查 docs/index.md 的 hero 区，把四根柱子的 emoji 换成更有 HF 味道的（🔼✅🌱🔗 可以换成 🤗🎨🧠📚 之类）；词条页的 h2 标题可以考虑 emoji 前缀装饰（但要克制，不能干扰阅读——上次加 ◆ 字符被用户嫌弃像感叹号，这次只用真正的 emoji 且加在标题文字前，不用伪元素字符）
3. **玻璃态 + HF 多彩 pill**：hero 区的 pill 保持多彩（每个柱子一个 HF 色，已有雏形），优化对比度和可读性
4. **阅读体验优先**：任何装饰不得影响正文阅读。用户明确抱怨过"装饰字符影响阅读"，所以：
   - 禁止用 ◆ ★ ● 等 Unicode 字符做装饰（会渲染成奇怪的符号）
   - emoji 只放在标题文字前（如 "## 🤖 原理"），不用 CSS content 注入
   - 配色对比度要够（深色底上文字清晰）
5. **移动端适配**：检查小屏幕下 hero/pill/卡片是否正常

完成后报告：修改了哪些文件、每个改动的理由、以及你判断的视觉效果。不要跑 mkdocs build（我来验证）。